<%= content_tag(:h2, t("plebisbrand.user.wants_info_by_sms"))%>
<%= semantic_form_with model: resource, scope: resource_name, url: registration_path(resource_name)+"#wants-info-by-sms", html: { novalidate: true } do |f| %>
  <fieldset xmlns="http://www.w3.org/1999/html">
    <div class="inputlabel-box">
      <% if resource.errors.include?(:born_at) %>
        <%= field_notice_box %>
      <% end %>
      <%= f.check_box :wants_information_by_sms, class: 'checkbox' %>
      <%= f.label :wants_information_by_sms, class: 'text-xl' do %>
        <%= t 'plebisbrand.user.wants_info_by_sms_text'%>
      <% end %>
      <p></p>
      <p class="text-xl">(Si desactivas la casilla, dejarás de recibir SMS de tipo informativo, sobre votaciones en curso o sobre eventos de PlebisBrand, pero seguirás recibiendo los SMS que utilizamos como medida de seguridad para acceder a las cabinas de votación)</p>
    </div>
    <div class="inputlabel-box">
      </br>
      <%= f.input :current_password,
                  required: true,
                  hint: raw(t("devise.hints.current_password", :link => link_to(t("devise.links.here"), "#recover-password", class: "js-change-tab"))),
                  input_html: { class: 'input-m' } %>
    </div>
    <div class="enter">
        <%= f.submit t("devise.buttons.edit"), class: "button" %>
      </div>
  </fieldset>
<% end %>


