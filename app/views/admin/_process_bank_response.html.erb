<%= form_with scope: :process_bank_response, url: { action: "process_bank_response" }, local: true do |f| %>
    <%= f.file_field :file %>
    <div class="buttons">
      <%= f.submit 'Submit', class: 'button' %>
    </div>
<% end %>
